# Hosting (React + Vite)

This folder contains the Partnership Mapping frontend.

## Docs

- Repo overview + setup: `../README.md`
- Orientation + entry points: `../START_HERE.md`
- Component interfaces: `../component.md`

## Commands

- Dev server: `npm run dev`
- Lint: `npm run lint`

Deploy from repo root:

- `firebase deploy --only hosting`
